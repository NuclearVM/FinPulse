services:
  timescaledb:
    image: timescale/timescaledb-ha:pg18
    container_name: timescaledb

    ports:
      - "127.0.0.1:5432:5432"

    environment:
      POSTGRES_PASSWORD: password
      PGDATA: /pgdata

    volumes:
      - ./timescaledb:/pgdata